# Milestone: MS8 addendum

## Metadata

- milestone_id: MS-20260528-003
- title: MS8 addendum
- milestone_kind: addendum
- status: active
- priority: 10
- created_by: fdch0
- created_at: 2026-05-28
- updated: 2026-05-28
- updated_by: harness-kernel
- activated_by: fdch0
- activated_at: 2026-05-28
- activation_note: activated only; no worktrack initialized by request.

## Purpose

在 MS8 的多知识库与 AI 草稿范围能力稳定后，补齐两个自然延伸：管理员对知识库本身的生命周期管理，以及 AI 追问从多个方向组织问题的能力。该 addendum 不改变 MS8 已确认的“知识库可选择、AI 范围过滤、单草稿确认边界”，而是在其上提升可维护性和对话质量。

## Worktrack List

| # | worktrack_id | title | node_type | status |
|---|-------------|-------|-----------|--------|
| 1 | WT-20260528-064 | 知识库编辑与禁用归档 | feature | planned |
| 2 | WT-20260528-065 | AI 多方向追问策略 | feature | planned |
| 3 | WT-20260528-066 | MS8 addendum 集成验收 | test | planned |

## Completion Signals

1. 管理员可以编辑知识库名称、描述和启用状态；slug 或系统标识的修改必须有兼容边界。
2. 知识库支持禁用/归档语义，禁用后不出现在普通用户 AI 选择范围中，不参与 scoped retrieval。
3. 第一版不做有内容知识库的物理删除；如后续需要删除，只允许空知识库或另开明确确认边界。
4. 默认知识库必须保留，不能被删除或破坏性禁用到导致历史来源不可管理。
5. AI 追问可以按方向组织，至少覆盖：知识库/参考资料角度、应用场景角度、具体需求细节角度。
6. 多方向追问仍必须保持人工确认边界，不得直接创建或修改工单。
7. 追问方向应在前端可读地呈现，用户可以理解每个问题为什么被问。
8. `npm run lint`、`npm run test`、`npm run build` 通过，且 Milestone DB Readiness Gate 通过。

## Acceptance Criteria

1. 管理员能从知识库管理界面编辑知识库基本信息和启用/归档状态。
2. 禁用/归档知识库后，上传目标、AI 页面选择项和检索结果都遵循同一 enabled 语义。
3. 有来源内容的知识库不会被误物理删除；默认知识库有明确保护。
4. AI 追问结果包含方向信息，且 UI 能区分或展示方向，例如“知识库依据 / 应用场景 / 需求细节”。
5. 追问方向不会破坏现有 clarification response 兼容性；旧字段仍可被安全处理。
6. 多方向追问后生成草稿仍走现有单选人工确认流程。
7. Prisma schema/migration、当前开发数据库状态、管理员知识库 API readiness 必须在最终 handback 前通过。

## Completion Threshold

- completion_threshold_pct: 100

## Dependencies

- depends_on_milestones: [MS-20260528-001]

## Progress Counter

- total_worktracks: 3
- completed: 0
- blocked: 0
- deferred: 0

## Requirement Notes

- fdch0 confirmed `禁用归档 + 可编辑` is preferable to broad knowledge-base deletion.
- fdch0 requested AI clarification from multiple directions: knowledge-base angle, application-scenario angle, and concrete requirement-detail angle.
- This addendum is waiting for focused programmer clarification before WT-20260528-064 is initialized.

## Pending Clarification Questions

1. 知识库禁用与归档是否需要拆成两个状态？建议第一版使用单一 `enabled` 开关并在 UI 文案中表达为“禁用/归档”，除非你需要“临时隐藏”和“长期封存”两个可区分状态。
2. 默认知识库是否允许被禁用？建议默认知识库不可删除，且只有在至少还有一个可用知识库时才允许禁用；如果禁用默认库，历史来源仍可在管理员界面查看。
3. 知识库名称/描述可编辑时，slug 或系统标识是否保持不可改？建议保持不可改，避免破坏历史引用、上传路径、citation 和 scoped retrieval。
4. 禁用/归档知识库是否仍允许管理员上传新来源？建议禁用后不允许普通新增内容，只允许查看、恢复启用、编辑元信息和清理已有来源。
5. AI 多方向追问的输出形式是固定三组问题，还是允许 provider 按上下文省略某些方向？建议固定三组方向，但每组可返回 0 到 N 个问题并说明无问题时的原因。
6. 多方向追问在 UI 上是否要求用户逐方向回答，还是仍保留一个统一回答区？建议保留统一回答区，同时按方向展示问题，避免引入复杂对话状态机。
7. 多方向追问与“最多 3 个拆分草稿”如何组合？建议追问阶段只收集澄清，草稿阶段仍按现有变量最多生成 3 个候选/拆分草稿，且仍只能选择一个交给工单表单。
8. 方向标签是否固定为“知识库依据 / 应用场景 / 需求细节”？建议固定第一版标签和 API enum，后续再开放配置。
9. 最终验收是否需要包含手工 UI 流程记录？建议需要，覆盖禁用知识库不出现在 AI 选择、恢复启用后可再次检索、多方向追问显示与单草稿 handoff。

## Milestone Gate Design

- black_box: 管理员能维护知识库生命周期；用户能看到多方向 AI 追问并继续生成草稿。
- white_box: enabled/archived 语义在 admin UI、upload target、public bases API、retrieval 和 citations 中一致； clarification schema 兼容旧调用。
- anti_cheat: 不允许误删有内容知识库；不允许禁用后继续被 AI 检索；不允许多方向追问绕过人工确认；不允许跳过 Milestone DB Readiness Gate。

## Developer Decision Boundary

- 物理删除非空知识库、批量迁移来源到其它知识库、重写 slug 策略、自动选择追问方向或引入复杂对话状态机，需要单独确认。
- 如果多方向追问需要改变 provider schema 或 UI 交互较大，应优先保持兼容扩展，不做破坏性 schema 替换。
- 在上述 pending clarification questions 得到回答前，不初始化 WT-20260528-064。
