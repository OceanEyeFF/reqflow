# Local development defaults. Copy this file to `.env` before running
# Prisma validation, database seed, or the Next.js dev server.
DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-local-development-secret"
